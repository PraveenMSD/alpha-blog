<div class="card-text text-left">
    <h6>Comments:</h6>
    <% @article.comments.each do |comment| %>
    <p>
        <div id="edit_comment_<%= comment.id %>">
            <b><%= comment.commenter %> :</b>
            <%= comment.body %>
            <%= link_to "Delete comment", [comment.article, comment], method: :delete, remote: true %>
        </div>
    </p>
    <% end %>
</div>
